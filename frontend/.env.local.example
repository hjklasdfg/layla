# Copy to .env.local:  cp .env.local.example .env.local

# ─── TfL Unified API (server-only — never NEXT_PUBLIC_) ─────────────────────
# Register at https://api-portal.tfl.gov.uk/
TFL_APP_KEY=your_tfl_primary_key_here
TFL_APP_ID=

# ─── Layla backend ───────────────────────────────────────────────────────────
# Your backend base URL, e.g. http://localhost:8000
BACKEND_API_URL=https://layla.ai-cloud.io
BACKEND_API_KEY=

# ─── Live hazard watch ───────────────────────────────────────────────────────
# Loop test (no backend): real camera frames → faked bboxes → stop after ~30 frames (~9s)
CAMERA_HAZARD_FAKE_LOOP=true
# CAMERA_HAZARD_FAKE_AT_FRAME=30

# Real-time tuning (client-side, optional)
# NEXT_PUBLIC_CAMERA_HAZARD_FRAME_MS=300
# NEXT_PUBLIC_CAMERA_HAZARD_FRAME_WIDTH=640

# Real inference (YOLO backend) — disable fake loop first:
# CAMERA_HAZARD_FAKE_LOOP=false
# CAMERA_HAZARD_API_URL=http://127.0.0.1:8001
# CAMERA_HAZARD_AUTO_START=false
# YOLO_MODEL=yolo11n.pt

# LLM provider: gemini | nemotron | backend
LLM_PROVIDER=gemini
NEXT_PUBLIC_LLM_PROVIDER=gemini

# ─── Nemotron inference (OpenAI-compatible vLLM) — mobility planning ───────
NEMOTRON_BASE_URL=https://theory-refresh-soma-provisions.trycloudflare.com
NEMOTRON_MODEL=nvidia/Nemotron-3-Nano-Omni-30B-A3B-Reasoning-NVFP4
# NEMOTRON_API_KEY=

# ─── Google Gemini (optional — mobility/other features) ──────────────────────
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash
GEMINI_VISION_MODEL=gemini-2.5-flash

# ─── Hazard report (NemoClaw agent first, Nebius fallback) ───────────────────
# Primary: standalone backend/layla-nemoclaw (auto-starts on :8002 by default)
# LAYLA_NEMOCLAW_URL=http://127.0.0.1:8002
# LAYLA_NEMOCLAW_AUTO_START=true
# LAYLA_NEMOCLAW_DEMO=true
# Nebius-only (skip NemoClaw): LAYLA_NEMOCLAW_AUTO_START=false
# Fallback: Nebius AI — used when NemoClaw agent is down or unreachable
# Get a key at https://studio.nebius.ai/
NEBUISAI_API_KEY=your_nebius_api_key_here
# NEBUISAI_BASE_URL=https://api.tokenfactory.nebius.com/v1
# NEBUISAI_MODEL=Qwen/Qwen3-32B
# NEBUISAI_VISION_MODEL=Qwen/Qwen2.5-VL-72B-Instruct
# Pretend GPS for hazard report when browser location is unavailable (landmark id)
# HAZARD_REPORT_FALLBACK_LOCATION=st-pancras
# Optional: Tavily search — better web results than DuckDuckGo fallback

# Optional: auto-send hazard report emails via Resend (https://resend.com)
# RESEND_API_KEY=
# HAZARD_REPORT_FROM_EMAIL=Layla <reports@yourdomain.com>

# ─── ElevenLabs voice (Scribe STT + TTS) ─────────────────────────────────────
# Tap mic to start/stop speech input; Layla TTS reads plan results aloud.
# TTS speaks plan voiceText after planning (POST /api/voice/speak).
ELEVENLABS_API_KEY=
NEXT_PUBLIC_VOICE_ENABLED=true
ELEVENLABS_VOICE_ID=
ELEVENLABS_TTS_MODEL=eleven_multilingual_v2

# ─── Voice transport mode ────────────────────────────────────────────────────
# "scribe" = legacy push-to-talk STT (default, kept until ConvAI proven).
# "conversational" = ElevenLabs Conversational AI, full-duplex, routed through
# the OpenClaw agent via /api/voice/chat. Requires NEXT_PUBLIC_ELEVENLABS_AGENT_ID
# and an ElevenLabs agent configured with Custom-LLM URL = <tunnel>/api/voice/chat.
NEXT_PUBLIC_VOICE_MODE=scribe
# ElevenLabs Conversational AI agent id (Conversational AI mode only).
NEXT_PUBLIC_ELEVENLABS_AGENT_ID=

# ─── OpenClaw agent gateway (Conversational AI mode) ─────────────────────────
# /api/voice/chat bridges ElevenLabs Custom-LLM to the OpenClaw gateway WS RPC.
# Token from <openclaw home>/openclaw.json -> gateway.auth.token (host:
#   jq -r .gateway.auth.token ~/.openclaw/openclaw.json )
OPENCLAW_GATEWAY_URL=ws://127.0.0.1:18789/ws
OPENCLAW_GATEWAY_TOKEN=
OPENCLAW_MODEL=nvidia/Nemotron-3-Nano-Omni-30B-A3B-Reasoning-NVFP4

# Shared secret ElevenLabs Custom-LLM sends as `Authorization: Bearer <secret>`
# to /api/voice/chat. REQUIRED in production (public tunnel drives a GPU agent).
VOICE_CHAT_SECRET=

# ─── Backend mobility contract ───────────────────────────────────────────────
# Next.js sends raw TfL candidates + sensors; backend enriches OSM and returns
# path options + explanation (uiText for panel, voiceText for voice guide).
# POST {BACKEND_API_URL}/mobility/plan
