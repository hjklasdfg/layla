// Test stub: `server-only` throws when imported outside a server bundle, which
// breaks unit tests in vitest's node environment. Aliased to this no-op in
// vitest.config.ts so server-only modules can be unit tested.
export {};
